[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"

[project]
name = "zhoudao"
version = "0.0.10rc1"
description = "中文句法式 Python 方言"
readme = "README.md"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
include = ["周道*"]
