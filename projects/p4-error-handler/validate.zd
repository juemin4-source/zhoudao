定义验证年龄（年龄）如下：
如果年龄小于0，就报错【年龄不得为负】，错误类型是值出错。
如果年龄大于150，就报错【年龄超过上限】，错误类型是值出错。
以年龄为所得。

定义查询用户（编号）如下：
如果编号等于0，就报错【用户不存在】，错误类型是键出错。
以编号为所得。

定义处理（编号、年龄）如下：
尝试设用户为查询用户（编号）；
设验证为验证年龄（年龄）；
如果错误类型是键出错，就显示【未找到用户】；
如果错误类型是值出错，就显示【参数无效】；
如果出错，就原样报出当前错误。

运行如下：
处理（1、25）。
