设置信号灯类别，包括状态，默认为【红】。

定义信号灯类别的切换（）如下：
如果自己的状态等于【红】，就使自己的状态变为【绿】；
不然如果自己的状态等于【绿】，就使自己的状态变为【黄】；
不然使自己的状态变为【红】。

定义信号灯类别的显示状态（）如下：
显示自己的状态。

运行如下：
设灯为信号灯（）。
灯的显示状态（）。
灯的切换（）。
灯的显示状态（）。
灯的切换（）。
灯的显示状态（）。
灯的切换（）。
灯的显示状态（）。