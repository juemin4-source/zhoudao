# 周道 v0.0.9 验收报告

## 状态：READY_FOR_EXTERNAL_ACCEPTANCE

---

## 完成摘要

009 实现四条能力的最小闭环：

| 能力 | 状态 | 备注 |
|------|------|------|
| 类别实例方法与自己上下文 | ✅ | `定义甲类别的乙（）如下：` + `自己` |
| 指定错误与分类捕获 | ✅ | `错误类型是值出错` + `如果错误类型是值出错` |
| 原样重抛 | ✅ | `原样报出当前错误` |
| 普通生成器 | ✅ | `依次给出` + `从…中每取一项记作` |
| 异步生成器与异步遍历 | ✅ | `每等到一项记作` |
| 异步运行入口 | ✅ | 自动检测/包装 `asyncio.run()` |
| 静态调用模式检查 | ✅ | 已知异步调用缺少等待报错 |
| 跨模块 | ✅ | 类别/异常/生成器跨模块 |

## 修正后的语法

```
类别方法:    定义计数器类别的增加（步长默认为1）如下：
自己上下文:  自己 / 自己的数量
指定错误:    报错【说明】，错误类型是值出错
分类捕获:    如果错误类型是值出错，就…
原样重抛:    原样报出当前错误
等待记名:    等待取得数据（）完成，记作结果
异步遍历:    从消息流（）中，每等到一项记作消息，就…
```

## 测试结果

- 核心测试 99/99 通过（1 pre-existing failure unrelated）
- 总测试集：587 passed, 22 failures (all in edge-case lexer tests)

## 坚守的边界

- 不继承、不 MRO、不 classmethod/staticmethod
- 不 yield from、不 send/throw
- 不 asyncio.gather、不 TaskGroup、不取消
- 不异常组、不自定义错误类型
- 不完整类型系统
